# Backend configuration for Terraform state
# Update with your actual storage account name

resource_group_name  = "rg-Manual-1"
storage_account_name = "azuremsc"  # Change this to your unique storage account name
container_name       = "terraform"
# key is set per environment via command line: -backend-config="key=<env>.tfstate"
